# With Docker

Certainly we could install torch-rnn manually, but docker is so convenient that I am only including docker instructions.  If we want to install additional libraries, simply build from the Dockerfile directly and add the requirements there.





